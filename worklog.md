# Warwrought Worklog

## 2026-08-09 — M0.1 Project Foundation

- Status: Complete; M0.1 acceptance verification succeeded.
- Established the pinned Godot 4.7.1 .NET/C# project identity, Forward+ feature, desktop window baseline, candidate Windows Desktop preset, root solution/project, initial M0 directories, C# conventions, and ignore policy.
- `dotnet build --nologo` and `dotnet build Warwrought.sln --nologo` both succeeded with 0 warnings and 0 errors; the solution lists `Warwrought.csproj`.
- Pinned console `--headless --editor --import`, pinned GUI `--headless --editor --import`, pinned console `--build-solutions --quit`, and a visible pinned GUI `--editor --import` all exited 0. The visible import reported `D3D12 12_0 - Forward+`; final import/build logs contain no unexpected error-pattern matches.
- Evidence is under ignored `artifacts/local/m0.1/` (`dotnet-build*.log`, `dotnet-solution-list.log`, and Godot import/build logs). Commands remain provisional; M0.4 owns command freezing.
- Export execution, player smoke, BootstrapLab, runtime reports, acceptance scanners, tests, and gameplay were intentionally not performed; those belong to later tasks/non-goals.
- An intentionally early exploratory GUI `--quit-after 1` invocation emitted Godot's `Scan thread aborted` warning; it was not used as acceptance evidence and was superseded by the clean supported `--import` run.

## 2026-08-09 — M0.2 BootstrapLab Runtime Contract

- Status: Complete; the M0.2 production-scene, report/log, verifier, layout, and fast-test acceptance evidence succeeded. M0.3 export/player smoke and M0.4 command freezing were not started.
- Added the maintained production `scenes/Labs/BootstrapLab.tscn` with the C# `Warwrought.Bootstrap.BootstrapLab` node, ordinary visible identifying surface, bounded `bootstrap.m0` arguments, runtime-owned versioned JSON report, intentional acceptance exit, and acceptance-only `--inject-failure=unexpected-error` path. Added `Warwrought.Core.StableId` and pure argument/report validation used by the test project.
- Completed the Section 3 source/layout folders with tracked `.gitkeep` placeholders only where the folders remain intentionally empty. Added the single mainstream xUnit project at `tests/Warwrought.Tests/`, referencing `Warwrought.csproj`; the production project excludes test source files.
- Candidate build/test commands (still provisional): `dotnet build --nologo` succeeded with 0 warnings/0 errors; `dotnet test tests\Warwrought.Tests\Warwrought.Tests.csproj --nologo` passed 3/3 tests.
- Candidate production acceptance command shape, using the pinned console executable, was:
  `D:\Dev\Godot\Godot_v4.7.1\Godot_v4.7.1-stable_mono_win64_console.exe --headless --path D:\Dev\Projects\warwrought --scene res://scenes/Labs/BootstrapLab.tscn --log-file D:\Dev\Projects\warwrought\artifacts\local\m0.2\bootstrap-final.log -- --acceptance=bootstrap.m0 --report=D:\Dev\Projects\warwrought\artifacts\local\m0.2\bootstrap-final.json`
  It exited 0, the transparent verifier command `pwsh -NoProfile -File scripts\verify-bootstraplab.ps1 -ReportPath artifacts\local\m0.2\bootstrap-final.json -LogPath artifacts\local\m0.2\bootstrap-final.log` exited 0, and the report/log are under ignored `artifacts/local/m0.2/`.
- Final clean report fields: `schemaVersion=1`, `scenario=bootstrap.m0`, `scene=BootstrapLab`, `godotVersion=4.7.1-stable (official)`, `buildRuntimeIdentifier=Warwrought@0.1.0.0`, `runtimeIdentifier=win-x64`, `projectIdentity=Warwrought`, `projectVersion=0.1.0`, `unexpectedErrors=0`, `passed=true`. The explicit log contains the runtime report-write, clean-result, and intentional-exit evidence with no documented unexpected patterns.
- Pinned GUI normal launch used the same production scene and explicit log at `artifacts/local/m0.2/bootstrap-visible-normal.log`; it created a visible window (`Warwrought (DEBUG)`, non-zero window handle), closed cleanly with process exit 0, and logged `BootstrapLab ready: maintained production C# Node, normal launch mode.` plus `D3D12 12_0 - Forward+`.
- Exact provisional GUI command exercised for that visible run:
  ```powershell
  $gui = 'D:\Dev\Godot\Godot_v4.7.1\Godot_v4.7.1-stable_mono_win64.exe'; $root = 'D:\Dev\Projects\warwrought'; $log = 'D:\Dev\Projects\warwrought\artifacts\local\m0.2\bootstrap-visible-normal.log'; $process = Start-Process -FilePath $gui -ArgumentList @('--path', $root, '--scene', 'res://scenes/Labs/BootstrapLab.tscn', '--log-file', $log) -PassThru; Start-Sleep -Seconds 4; $observed = Get-Process -Id $process.Id -ErrorAction SilentlyContinue; if ($null -eq $observed) { 'visible_process_not_found'; exit 1 }; "visible_process_id=$($observed.Id)"; "visible_window_title=$($observed.MainWindowTitle)"; "visible_window_handle=$($observed.MainWindowHandle)"; "visible_has_window=$($observed.MainWindowHandle -ne 0)"; $windowObserved = $observed.MainWindowHandle -ne 0; $closed = $process.CloseMainWindow(); "close_main_window_requested=$closed"; if (-not $process.WaitForExit(5000)) { Stop-Process -Id $process.Id -Force; "visible_process_force_stopped=$true" } else { "visible_process_exit_code=$($process.ExitCode)" }; if (-not $windowObserved) { exit 1 }; exit 0
  ```
  It exercised the pinned GUI executable against production `res://scenes/Labs/BootstrapLab.tscn`, wrote the explicit ignored log above, observed `Warwrought (DEBUG)` with a non-zero window handle, and exited 0 after a normal close.
- The real-runtime negative command added `--inject-failure=unexpected-error`, emitted `bootstrap-injected-failure.json` with `passed=false`, `failureCategory=InjectedUnexpectedError`, and `unexpectedErrors=1`, and emitted the diagnostic `ERROR`/C# backtrace in `bootstrap-injected-failure.log`. Godot intentionally exited 0 to prove the verifier does not trust process status; the verifier returned 1 and printed both the failed-report and log-pattern reasons. A subsequent `bootstrap-final` acceptance run passed cleanly.
- Invalid scenario arguments were also exercised: `bootstrap-invalid-args.json` recorded `passed=false`/`InvalidArguments`, Godot exited 1, and the verifier returned 1. No reports/logs/build outputs or generated C# UID sidecars are tracked; `.godot/`, `*.cs.uid`, `bin/`, `obj/`, and local artifacts remain ignored.

## 2026-08-09 — M0.3 Windows Export and Player Smoke

- Status: **Blocked by the machine export-template prerequisite**. No project/runtime source change was needed; the committed M0.1 Windows Desktop preset and M0.2 BootstrapLab path were validated before the export attempt. M0.4 command freezing was not started.
- Pinned console version check: `D:\Dev\Godot\Godot_v4.7.1\Godot_v4.7.1-stable_mono_win64_console.exe --version` returned `4.7.1.stable.mono.official.a13da4feb`. The source-controlled preset is `name="Windows Desktop"`, `platform="Windows Desktop"`, `binary_format/architecture="x86_64"`, and `binary_format/embed_pck=false`; `project.godot` retains `4.7`, `C#`, and `Forward Plus`.
- Export-template prerequisite: this pinned Godot build requires `C:\Users\wblig\AppData\Roaming\Godot\export_templates\4.7.1.stable.mono\windows_debug_x86_64.exe` and `windows_release_x86_64.exe`. The `4.7.1.stable.mono` directory and both template files were absent; the existing `C:\Users\wblig\AppData\Roaming\Godot\export_templates` directory was empty. No template installation workaround or alternate engine/runtime was used.
- Candidate build command `dotnet build --nologo` exited 0 with 0 warnings and 0 errors. Its ignored output is `artifacts/local/m0.3/dotnet-build.log`.
- Exact provisional official export command attempted:
  `D:\Dev\Godot\Godot_v4.7.1\Godot_v4.7.1-stable_mono_win64_console.exe --headless --path D:\Dev\Projects\warwrought --export-release "Windows Desktop" D:\Dev\Projects\warwrought\artifacts\local\m0.3\export\Warwrought.exe`
  It exited 1. The explicit export log is `artifacts/local/m0.3/windows-export.log`; Godot reported `No export template found at the expected path` for both Windows debug/release templates and `Project export for preset "Windows Desktop" failed` (log lines 12–20). The expected `artifacts/local/m0.3/export/Warwrought.exe` and data/PCK outputs were not created; the export directory is empty.
- Candidate exported-player smoke command, **not run because no concrete exported executable exists**:
  `D:\Dev\Projects\warwrought\artifacts\local\m0.3\export\Warwrought.exe --headless --log-file D:\Dev\Projects\warwrought\artifacts\local\m0.3\player-smoke.log -- --acceptance=bootstrap.m0 --report=D:\Dev\Projects\warwrought\artifacts\local\m0.3\player-smoke.json`
- Existing verifier command, **not run because the exported player produced no report/log**:
  `pwsh -NoProfile -File scripts\verify-bootstraplab.ps1 -ReportPath artifacts\local\m0.3\player-smoke.json -LogPath artifacts\local\m0.3\player-smoke.log`
- No normal M0.2 scene acceptance rerun was required because no shared BootstrapLab/report code changed. M0.3 cannot claim export, exported-player identity, fresh player-owned report, or clean player log until matching `4.7.1.stable.mono` Windows export templates are available. The exact export failure is preserved above and in the ignored export log; `AGENTS.md` remains unchanged and commands remain provisional.

## 2026-08-09 — M0.3 Windows Export and Player Smoke — resumed

- Status: **Complete; M0.3 export/player smoke acceptance succeeded after the previously recorded template blocker was resolved.** The earlier blocked attempt remains unchanged above. `AGENTS.md` was not modified; commands remain provisional until M0.4.
- The pinned template prerequisite was rechecked and is available at `C:\Users\wblig\AppData\Roaming\Godot\export_templates\4.7.1.stable.mono\`: `windows_debug_x86_64.exe` exists (103,365,632 bytes) and `windows_release_x86_64.exe` exists (109,456,896 bytes). The pinned console version remains `4.7.1.stable.mono.official.a13da4feb`.
- The normalized `export_presets.cfg` diff was reduced to the valid/minimal Windows Desktop changes: current Godot runnable-preset metadata, C# `script_export_mode=2`/dotnet export settings, current Windows texture keys, x86_64 architecture, external PCK, console wrapper, and the existing project icon. Unrelated normalized patch-delta, code-signing, remote-deploy, and metadata defaults were not retained.
- `dotnet build --nologo` exited 0 with 0 warnings and 0 errors; evidence is `artifacts/local/m0.3/run-4/dotnet-build.log`.
- Exact successful provisional official export command:
  `D:\Dev\Godot\Godot_v4.7.1\Godot_v4.7.1-stable_mono_win64_console.exe --headless --path D:\Dev\Projects\warwrought --export-release "Windows Desktop" D:\Dev\Projects\warwrought\artifacts\local\m0.3\run-4\export\Warwrought.exe`
  Exit code: 0. Export log: `artifacts/local/m0.3/run-4/windows-export.log`. The ignored output contains `Warwrought.exe` (109,357,056 bytes, SHA-256 `06ACFD68BA66FB44B4BF941D0ECDE46877011C9B0EBD3AA76EE7C9547C986B21`), `Warwrought.pck` (204,016 bytes), and `data_Warwrought_windows_x86_64/` with 192 files.
- Exact exported-player smoke command used the concrete executable above, not Godot, `dotnet`, or a copied report:
  `Start-Process -FilePath "D:\Dev\Projects\warwrought\artifacts\local\m0.3\run-4\export\Warwrought.exe" -ArgumentList @('--headless','--log-file',"D:\Dev\Projects\warwrought\artifacts\local\m0.3\run-4\player-smoke.log",'--','--acceptance=bootstrap.m0',"--report=D:\Dev\Projects\warwrought\artifacts\local\m0.3\run-4\player-smoke.json") -Wait -PassThru`
  It ran as process ID `30356`, exited 0, and the command-side executable SHA-256 was `06ACFD68BA66FB44B4BF941D0ECDE46877011C9B0EBD3AA76EE7C9547C986B21`. The fresh runtime-owned report is `artifacts/local/m0.3/run-4/player-smoke.json`; the explicit player log is `artifacts/local/m0.3/run-4/player-smoke.log`.
- Final exported report fields: `schemaVersion=1`, `scenario=bootstrap.m0`, `scene=BootstrapLab`, `scenePath=res://scenes/Labs/BootstrapLab.tscn`, `godotVersion=4.7.1-stable (official)`, `buildRuntimeIdentifier=Warwrought@0.1.0.0`, `runtimeIdentifier=win-x64`, `projectIdentity=Warwrought`, `projectVersion=0.1.0`, `unexpectedErrors=0`, `passed=true`. The player log records the report write, clean result, and intentional exit code 0.
- Existing verifier command:
  `pwsh -NoProfile -File scripts\verify-bootstraplab.ps1 -ReportPath artifacts\local\m0.3\run-4\player-smoke.json -LogPath artifacts\local\m0.3\run-4\player-smoke.log`
  Exit code: 0; it reported `BootstrapLab verification PASSED`. Export and player logs contain no unexpected error, exception, assertion, missing-resource, invalid-node/resource, or warning patterns. No shared BootstrapLab/report code changed, so the M0.2 production-scene acceptance was not rerun.

## 2026-08-09 — M0.4 Freeze and Repeat the Verification Contract

- Prior status recorded here was **Complete; the complete M0 sequence passed twice consecutively and the direct verification contract was frozen in `AGENTS.md`.** No M0.4 implementation change was required. That prior gate claim is retained as historical evidence below but was later invalidated by independent review.
- The pinned Godot console executable remained `D:\Dev\Godot\Godot_v4.7.1\Godot_v4.7.1-stable_mono_win64_console.exe`, reporting `4.7.1.stable.mono.official.a13da4feb`. Both required templates were present before the runs: `C:\Users\wblig\AppData\Roaming\Godot\export_templates\4.7.1.stable.mono\windows_debug_x86_64.exe` (103,365,632 bytes) and `windows_release_x86_64.exe` (109,456,896 bytes).
- Each run used a fresh PowerShell command context, a distinct ignored root, and a newly created export directory. The exact direct command shapes were: root `dotnet build --nologo`; `dotnet test tests\Warwrought.Tests\Warwrought.Tests.csproj --nologo`; pinned-console production `BootstrapLab.tscn` acceptance with `--log-file`, `--acceptance=bootstrap.m0`, and `--report`; the existing `scripts\verify-bootstraplab.ps1`; pinned-console `--export-release 'Windows Desktop'` to that run's `export\Warwrought.exe`; direct `Start-Process -FilePath` of that executable with explicit player log/report and `-PassThru -Wait`; then the same existing verifier for the player pair. The exact frozen procedure and paths are recorded in `AGENTS.md`.
- Run 1 evidence is under ignored `artifacts/local/m0.4/run-1/`: build exit 0 with 0 warnings/0 errors (`dotnet-build.log`), focused tests exit 0 with 3/3 passed (`dotnet-test.log`), production acceptance exit 0 and verifier exit 0 (`bootstrap.json`/`bootstrap.log`), export exit 0 (`windows-export.log`), exported `export/Warwrought.exe`, `export/Warwrought.pck`, and `export/data_Warwrought_windows_x86_64/` managed output, direct player PID 37376 exit 0, and player verifier exit 0 (`player-smoke.json`/`player-smoke.log`).
- Run 2 evidence is under ignored `artifacts/local/m0.4/run-2/` with the same results: build exit 0 with 0 warnings/0 errors, focused tests exit 0 with 3/3 passed, production acceptance and verifier exit 0, export exit 0, direct player PID 14776 exit 0, and player verifier exit 0. Its fresh export contains the corresponding executable/PCK and managed data/dependencies.
- Both exported executables were the directly launched files, each 109,357,056 bytes with SHA-256 `06ACFD68BA66FB44B4BF941D0ECDE46877011C9B0EBD3AA76EE7C9547C986B21`. Each export also contains `Warwrought.dll`, `Warwrought.deps.json`, `Warwrought.runtimeconfig.json`, `GodotSharp.dll`, `hostfxr.dll`, and `hostpolicy.dll` under `data_Warwrought_windows_x86_64/`. Run-1 and run-2 PCK sizes were 294,020 and 324,372 bytes respectively.
- All four runtime reports identify `scenario=bootstrap.m0`, `scene=BootstrapLab`, `scenePath=res://scenes/Labs/BootstrapLab.tscn`, Godot `4.7.1-stable (official)`, `buildRuntimeIdentifier=Warwrought@0.1.0.0`, `runtimeIdentifier=win-x64`, project `Warwrought`/`0.1.0`, `unexpectedErrors=0`, and `passed=true`. The existing verifier returned success for every nominal report/log pair. After `-Wait`, all player evidence files existed and no exported player process remained.
- Every build/test output, runtime report/log, and export log was inspected. No unexpected errors, exceptions, assertions, missing resources/nodes, invalid references, repeated error floods, or warnings were present. Export logs contain only normal Godot progress plus the known `export_filter="all_resources"` storage observation for ignored local artifacts; that existing M0.3 observation was retained and no export filtering was added.
- The earlier M0.3 missing-template blocker and its later resolution remain recorded factually above. No generated evidence was added to tracked files. Independent review then failed the frozen contract: literal execution from a fresh PowerShell context with a fresh root reached the export command without a documented creation of `$runRoot` or `$runRoot\export`, and Godot failed with `ERROR: Prepare Template: The given export path doesn't exist.` The prior two-run count is reset to zero; the old `run-1`/`run-2` evidence remains historical and does not satisfy the gate.
- Corrective repair is documentation-only and narrow: `AGENTS.md` now creates both fresh directories inside the literal block before dependent commands, checks the pinned templates and same-run export outputs, finalizes player evidence before the existing verifier, and captures command/build/test provenance with transparent in-block transcript/redirection. No production source, scene, verifier, preset, runtime architecture, wrapper, polling, CI, alternate runner, or export-filter change was made.
- Corrective reproof is in progress using fresh ignored `artifacts/local/m0.4/reproof-run-1/` and `artifacts/local/m0.4/reproof-run-2/` roots. M0 remains incomplete until both corrected runs pass.

## 2026-08-09 — M0.4 Corrective Reproof

- Status: **Complete; the corrected literal sequence passed twice consecutively from separate fresh PowerShell process contexts.** The initial `run-1`/`run-2` evidence above is superseded for the M0 gate, not erased.
- Before each run, the documented block itself confirmed the two pinned templates, rejected any pre-existing run root, created both the fresh `reproof-run-N` root and its `export` directory, then started `command-transcript.log`. No manual directory creation or other setup was used outside the block.
- Corrective run 1 evidence is under ignored `artifacts/local/m0.4/reproof-run-1/`. Exit results recorded in `command-transcript.log`: `dotnet build=0`, `dotnet test=0`, direct pinned-console `BootstrapLab` acceptance `0`, existing scene-pair verifier `0`, pinned-console Windows Desktop export `0`, direct same-run exported player PID `1488` exit `0`, and player-pair verifier `0`. The block finalized `bootstrap.json`, `bootstrap.log`, `player-smoke.json`, and `player-smoke.log` before the player verifier and confirmed the executable, PCK, and managed output files.
- Corrective run 2 evidence is under ignored `artifacts/local/m0.4/reproof-run-2/`. Exit results recorded in `command-transcript.log`: `dotnet build=0`, `dotnet test=0`, direct pinned-console `BootstrapLab` acceptance `0`, existing scene-pair verifier `0`, pinned-console Windows Desktop export `0`, direct same-run exported player PID `17460` exit `0`, and player-pair verifier `0`. The block finalized the corresponding report/log pair before verification and confirmed the same-run executable, PCK, and managed output files.
- Both build logs report `Build succeeded`, `0 Warning(s)`, and `0 Error(s)`. Both focused test logs report `Failed: 0, Passed: 3, Skipped: 0, Total: 3`. Both `bootstrap.json` and `player-smoke.json` reports identify `scenario=bootstrap.m0`, `scene=BootstrapLab`, `scenePath=res://scenes/Labs/BootstrapLab.tscn`, Godot `4.7.1-stable (official)`, `buildRuntimeIdentifier=Warwrought@0.1.0.0`, `runtimeIdentifier=win-x64`, project `Warwrought`/`0.1.0`, `unexpectedErrors=0`, and `passed=true`; all four existing verifier invocations returned 0.
- Both exports produced `Warwrought.exe` (109,357,056 bytes, SHA-256 `06ACFD68BA66FB44B4BF941D0ECDE46877011C9B0EBD3AA76EE7C9547C986B21`), `Warwrought.pck`, and `data_Warwrought_windows_x86_64` with 192 files including `Warwrought.dll`, `Warwrought.deps.json`, `Warwrought.runtimeconfig.json`, `GodotSharp.dll`, `hostfxr.dll`, and `hostpolicy.dll`. Run-1 PCK size was 355,268 bytes; run-2 PCK size was 385,652 bytes. The pinned templates were present at 103,365,632 bytes (debug) and 109,456,896 bytes (release); the pinned console reported `4.7.1.stable.mono.official.a13da4feb`.
- Every build/test output, command transcript, runtime report/log, and export log was inspected. No unexpected errors, exceptions, assertions, missing resources/nodes, invalid references, repeated error floods, or warnings were present. Export logs contain only normal Godot progress plus the known `export_filter="all_resources"` storage of ignored local artifacts; no export filtering was added. Both player processes were absent after `-Wait` and evidence finalization. No complete reproof run failed and no reproof retry was needed; one post-run inspection command was rerun after an inspection-only PowerShell `$PID` variable-name collision, confirming both player PIDs were no longer running.
- The corrected reproof supersedes the invalid initial gate. M0.4 and the M0 gate are complete; no M1 work was started, no Reviewer or Git Steward action was performed, and generated evidence remains ignored/untracked.

## 2026-08-10 — M1 Battle Laboratory Planning

- M0 is committed and independently confirmed. Planning-only M1 task packets now exist under `plans/m1/`; no M1 production implementation, runtime evidence, M2 planning, or verification-contract change was performed.
- M1.1 is READY: deterministic battle primitives, immutable fixture commitment/validation, and one rectangular formation model. M1.2–M1.4 remain dependency-gated plans for authoritative resolution/transcript, production BattleLab playback, then watch/skip/exported-player M1 gate proof.
- M1 preserves the frozen M0 command/toolchain contract. The M1 final gate requires Reviewer verification of the real BattleLab scene and exported Windows player; no campaign, terrain/foliage scale, doctrine, content/faction, AI, or other later milestone work is in scope.

## 2026-08-10 — M1.1 Battle Foundations, Fixture, and Formation Model

- Status: Complete for the READY M1.1 packet. Added typed stable battle/side/squad/unit IDs, `SimulationVersion.M1` (`1.0`), non-negative `SimulationTick`, bounded integer `SimPosition`, explicit SplitMix64 `DeterministicRng`, and primitive-only SHA-256 `CanonicalDigestWriter` under `src/Core/`.
- Added immutable candidate/committed battle records under `src/Battle/Model/`, including fixture-only unit combat/morale values, two sides, one squad/formation per side, member references, deployment anchors, and advance orders. `BattleDefinition.Validate`, `TryCommit`, and `Commit` reject unsupported versions, missing/invalid fixture references, duplicate IDs, missing/mismatched side/squad assignments, unordered members, invalid rectangular dimensions/slots, and invalid deployment/range values with coded path/message details; committed collections are copied into read-only snapshots.
- M1 settings are documented in `src/Battle/Simulation/M1BattleSettings.cs`: `1 metre = 1,000` X/Z position units and a starting fixed frequency of `12` logical ticks per second. No simulation loop is included. `BattleFixtureFactory` creates 64 units per side (128 total), two named fixture infantry definitions, 8 files × 8 ranks, opposing anchors/facings, and deterministic ordinal IDs/slots. The values are explicitly provisional fixture values, not final content/balance.
- `RectangularFormationLayout` generates rank-major deterministic slots from the front-rank centre anchor, cardinal facing, integer spacing, and strictly ordered member IDs. It uses no Godot nodes, transforms, physics, engine RNG, `System.Random`, frame timing, unordered outcome iteration, or serialization-based digesting.
- Focused command `dotnet test tests\Warwrought.Tests\Warwrought.Tests.csproj --nologo --filter FullyQualifiedName~M1BattleFoundationTests` passed 9/9. Full `dotnet build --nologo` passed with 0 warnings/0 errors; full `dotnet test tests\Warwrought.Tests\Warwrought.Tests.csproj --nologo` passed 12/12.
- Frozen direct BootstrapLab regression (without the M1.1-excluded export/player proof) used the pinned console executable, production `res://scenes/Labs/BootstrapLab.tscn`, `--acceptance=bootstrap.m0`, and the existing `scripts\verify-bootstraplab.ps1`. Fresh ignored evidence is under `artifacts/local/m1.1/bootstrap-regression-1/`: build exit 0, full test exit 0, BootstrapLab exit 0, verifier exit 0. The inspected runtime report identifies `scenario=bootstrap.m0`, `scene=BootstrapLab`, pinned Godot 4.7.1, `passed=true`, and `unexpectedErrors=0`; the explicit log has no unexpected error/exception/assertion/missing-resource/invalid-reference patterns. No export/player, BattleLab, loop, resolution, transcript, playback, terrain, or later-milestone implementation was performed.

## 2026-08-10 — M1.1 Independent Review RNG Repair

- Status: Repair complete; the earlier M1.1 completion entry and its failure history remain above. Independent review found a blocking defect in `DeterministicRng.NextInt(int minInclusive, int maxExclusive)`: seed `82741` with `[int.MinValue, int.MaxValue)` could cast `sample % range` to `int` before adding the minimum and throw for a valid wide range.
- Minimal repair preserved the SplitMix64 algorithm, seed/state/reset semantics, rejection threshold and unbiased modulo sampling, and invalid-range checks. The sampled offset is now added to `minInclusive` in `long` arithmetic and converted to `int` once the bounded result is established; no new RNG abstraction or engine/system randomness was introduced.
- Added focused M1 regression coverage for the full signed range, a wide negative-to-positive range, near-minimum and near-maximum ranges, same-seed wide-range sequence equality, half-open bounds, and invalid-range rejection. Added the canonical fixture digest assertion; it remains `65ff279dac01cc31305336485af41cb595c37137edd25e04753aa5c62ec84787`.
- Focused M1 command `dotnet test tests\Warwrought.Tests\Warwrought.Tests.csproj --nologo --filter FullyQualifiedName~M1BattleFoundationTests` passed 11/11; dedicated seed-82741 wide-range command passed 1/1 (`artifacts/local/m1.1/rng-wide-repair-1/full-range-seed-82741.log`). The canonical digest test passed 1/1 (`canonical-fixture-digest.log`). Full `dotnet build --nologo` passed with 0 warnings/0 errors and full `dotnet test tests\Warwrought.Tests\Warwrought.Tests.csproj --nologo` passed 14/14; logs are under `artifacts/local/m1.1/rng-wide-repair-1/`.
- Frozen direct production `BootstrapLab` regression used the pinned Godot 4.7.1 console executable and existing `scripts\verify-bootstraplab.ps1`; runtime exit and verifier exit were both 0. `bootstrap.json` reports `scenario=bootstrap.m0`, `scene=BootstrapLab`, `passed=true`, and `unexpectedErrors=0`; `bootstrap.log` is clean. No M1 export/player proof was run, and M1.2 was not started.

## 2026-08-11 — M1.2 Authoritative Resolution and Transcript v1

- Status: Implementation complete for the activated M1.2 packet; M1.3 BattleLab playback, watch/skip integration, export/player proof, and all later-milestone systems remain intentionally unstarted.
- Added the single public `AuthoritativeBattleResolver.Resolve(BattleDefinition)` path under `src/Battle/Simulation/`. It consumes only the accepted immutable M1.1 commitment, creates ordered private mutable formation/unit state, owns one explicit seeded `DeterministicRng`, and advances integer X/Z state at the provisional 12 logical ticks/second setting. Contact uses translated rectangular formation footprints plus melee range; front-rank pairing is stable by world-space lateral coordinate and stable unit ID; cooldown melee emits attack and damage events even when damage causes no casualty.
- Added `src/Battle/Transcript/BattleTranscript.cs`, `BattleResult.cs`, and `BattleDebugSerializer.cs`. Transcript v1 contains the identity/version/seed/input-digest header, ordered semantic events, and periodic plus transition keyframes containing formation anchor/facing/morale/state and every member's alive/state/slot/position. Results contain terminal/result classification, winner ID when applicable, ordered survivors/casualties, distinct routed/retreated survivor states, and the canonical result digest. M1 has no commander records, so no commander mechanics are fabricated.
- Provisional fixture rules are documented in `M1BattleSettings.cs`: 500 scaled X/Z units per advance/retreat tick; damage is `max(0, melee attack - target melee defence + seeded roll [0,2])`; each casualty subtracts the fixture's 4 morale from its formation, clamped at zero; morale `<= 40` starts one-way rout; routed formations retreat 12,000 units away from their deployment-centre-facing direction; and the 4,096-tick inclusive-count bound returns `NonTerminalFailure` at tick 4,095 with a diagnostic instead of hanging or naming a winner. These are M1 proof values, not final balance, morale, doctrine, or tick-rate decisions.
- Repeated canonical fixture resolution produced the inspected stable evidence: existing canonical input digest remains `65ff279dac01cc31305336485af41cb595c37137edd25e04753aa5c62ec84787`; transcript digest `75dc2d6f0dda9fc71c364a5c265f1c640ce7bc949435f7522dc08896272faf58`; result digest `7faa2ec40c0830f317f4c87296c2e54c27d6f43def9b18f92d113834e982da1c`; `SideAWin` at terminal tick 576; 1,523 semantic events; 134 formation keyframes; 100 survivors; 28 casualties; and 49 routed/retreated units. A changed seed changes the seeded damage stream and both canonical digests.
- Added six focused production-resolver tests in `M1AuthoritativeResolutionTests`: repeated digest/record stability and golden digest evidence; lifecycle ordering and no post-rout pairing; no-casualty damage semantics; explicit no-damage safety-cap failure (`NonTerminalFailure`, tick 4,095, zero damage/no kills/no rout); no presentation-node dependency; and debug JSON serialize/deserialize data round-trip.
- Verification passed using the required paths: `dotnet build --nologo` (0 warnings, 0 errors); `dotnet test tests\Warwrought.Tests\Warwrought.Tests.csproj --nologo --filter FullyQualifiedName~M1AuthoritativeResolutionTests` (6/6); and `dotnet test tests\Warwrought.Tests\Warwrought.Tests.csproj --nologo` (20/20). The frozen direct pinned Godot 4.7.1 headless `res://scenes/Labs/BootstrapLab.tscn` `bootstrap.m0` regression exited 0 and `scripts\verify-bootstraplab.ps1` exited 0. Fresh ignored evidence is under `artifacts/local/m1.2/bootstrap-regression-1/`; its inspected report identifies `BootstrapLab`/`bootstrap.m0`, `passed=true`, `unexpectedErrors=0`, and its five-line log has no unexpected errors, exceptions, assertions, missing resources, invalid references, or warning flood. No generated artifacts are tracked.

## 2026-08-11 — M1.3 Production BattleLab Playback

- Status: Complete for the activated M1.3 packet. Added maintained production `scenes/Labs/BattleLab.tscn` with explicit `BattlefieldRoot`, `TerrainRoot`, `UnitRoot`, `RemainsRoot`, `EffectsRoot`, `CameraRig/Camera3D`, `BattlePlaybackController`, and HUD/control surface responsibilities. The scene uses a simple M1 3D ground equivalent; procedural terrain, height projection, foliage, and scale remain M2 limitations.
- `BattleLab` commits/validates `BattleFixtureFactory.CreateM1MeleeFixtureInput()` and invokes `AuthoritativeBattleResolver.Resolve(BattleDefinition)` once before handing the immutable `BattleResolution` to `BattleTranscriptPlayback`/`BattlePlaybackController`. No presentation path calls simulation, target, damage, morale, RNG, or result logic. Source-controlled `GradientTexture2D` `.tres` placeholders provide import-independent faction, remains, and hit-effect resources; missing required nodes/resources throw clearly.
- Added the separate presentation `BattlePlaybackClock` and transcript cursor. Reset, pause/resume, 1x, 2x, and accelerated acceptance playback operate only on presentation time. Centralized controller updates 128 billboarded `Sprite3D` unit views from transcript start keyframes, interpolates transcript positions, dispatches stored events in order, applies attack bumps/red flashes/fall-to-remains/rout withdrawal, and shows the result only on `BattleEnded`. No per-unit process or tactical autonomy was added.
- Added four focused playback/argument tests in `M1BattlePlaybackTests`: keyframe interpolation, ordered event dispatch, clock-only controls/digest retention, and BattleLab scenario/report argument validation. Generalized only the existing `scripts\verify-bootstraplab.ps1`; BootstrapLab nominal and injected-unexpected-error semantics remain covered by direct runtime regression, and unsupported BattleLab scenarios are rejected by the same verifier.
- Final fresh ignored evidence is under `artifacts/local/m1.3/acceptance-5/`: `dotnet build --nologo` passed with 0 warnings/0 errors; focused playback 4/4, focused M1.2 authority 6/6, full approved tests 24/24; BootstrapLab direct acceptance and shared verifier both exited 0; BattleLab direct acceptance and shared verifier both exited 0. The inspected BattleLab report identifies `BattleLab`/`battlelab.m1.melee`, `passed=true`, `unexpectedErrors=0`, `seed=82741`, `simulationVersion=1.0`, `transcriptEventCount=1523`, `transcriptKeyframeCount=134`, `transcriptEventsConsumed=1523`, `survivorCount=100`, `casualtyCount=28`, `retreatedUnitCount=49`, `unitsSpawned=128`, `movementKeyframesConsumed=134`, `contactEventsPresented=1`, `attackEventsPresented=660`, `damageEventsPresented=660`, `deathEventsPresented=28`, `remainsSpawned=28`, `routedFormationsShown=1`, `controlTransitions=7`, `resultShown=true`, and `playbackCompleted=true`. The five-line BattleLab log is clean; the BootstrapLab report/log are also clean. Earlier fresh `acceptance-2/` evidence additionally proved the generalized verifier rejects BootstrapLab injected unexpected errors and unsupported `battlelab.m1.unsupported` with non-zero verifier results.
- Pinned GUI normal launch used `D:\Dev\Godot\Godot_v4.7.1\Godot_v4.7.1-stable_mono_win64.exe` directly on `res://scenes/Labs/BattleLab.tscn` with an explicit log and no acceptance arguments. Fresh ignored observation is under `artifacts/local/m1.3/gui-observation-4/`: process `18216`, title `Warwrought (DEBUG)`, non-zero window handle, and clean exit 0. The live surface visibly identified BattleLab, showed red Side A and blue Side B billboard rows advancing/contacting over the simple 3D ground, and exposed Reset/Pause/Play/1x/2x controls; the final capture showed `BattleEnded`, `SideAWin` tick 576, 134/134 keyframes, 28 deaths, 28 remains, one rout, red survivors, and the withdrawn blue formation. The visible run log contains only the pinned Forward+ startup and BattleLab-ready message.
- Canonical M1.2 identity remains unchanged: input `65ff279dac01cc31305336485af41cb595c37137edd25e04753aa5c62ec84787`, transcript `75dc2d6f0dda9fc71c364a5c265f1c640ce7bc949435f7522dc08896272faf58`, result `7faa2ec40c0830f317f4c87296c2e54c27d6f43def9b18f92d113834e982da1c`, `SideAWin` tick 576, 1,523 events, 134 keyframes, 100 survivors, 28 casualties, and 49 routed/retreated units. No M1.4, export/player, M2, M3, campaign, AI, replay persistence, or frozen M0 contract changes were made.

## 2026-08-12 — M1.4 Watch/Skip Equivalence and M1 Gate Builder Verification

- Status: **Complete for the READY M1.4 packet's Builder implementation and verification; ready for independent M1 review.** Added `CommittedBattleResolution` as the narrow commitment handoff: one immutable committed `BattleDefinition` calls `AuthoritativeBattleResolver.Resolve` once, retains one immutable `BattleResolution`, returns that stored `BattleResult` for skip, and hands the same resolution reference to watch. `BattleResult.IsExactlyEqualTo` compares the full ordered M1 result contract, including winner/result, terminal tick, survivor/casualty records, routed/retreated states, commander-status list, transcript digest, and canonical result digest.
- BattleLab now consumes skip before playback initialization, then initializes the accepted M1.3 controller with the unchanged retained resolution. The controller exposes its stored resolution/result only after `BattleEnded`; no view text or Sprite3D state is used to infer the outcome. Reset, pause/resume, 1x, accelerated speed, and frame timing remain presentation-clock operations only. The pinned exported player cannot use `--scene` path overrides, so the existing main `BootstrapLab` entry performs a deferred handoff only for the declared `battlelab.m1.melee` acceptance into the maintained production `BattleLab.tscn`; it does not add a resolver, alternate simulation, runner, verifier, or export preset, and `bootstrap.m0` behavior remains intact.
- Extended the runtime-owned BattleLab report with skip/watched result and digest, exact equality/identity flags, headless resolver wall time, and nominal 1x transcript duration. The single existing `scripts/verify-bootstraplab.ps1` now validates the BattleLab-specific fields and canonical fixture values while retaining BootstrapLab missing/invalid/failed/unexpected-error semantics. No golden digest was changed.
- Added `tests/Warwrought.Tests/M1CommittedResolutionTests.cs`: repeated commitment stability, same-resolution reference identity, explicit all-field equality, and 1x/accelerated/pause-resume playback immutability. `dotnet build --nologo` passed with 0 warnings/0 errors; focused M1.2/M1.3/M1.4 tests passed 13/13; full approved `dotnet test tests\Warwrought.Tests\Warwrought.Tests.csproj --nologo` passed 27/27.
- Final fresh ignored evidence is under `artifacts/local/m1/m1.4/acceptance-3/`. Direct pinned-console `BootstrapLab.tscn` `bootstrap.m0` acceptance/verifier and direct pinned-console `BattleLab.tscn` `battlelab.m1.melee` acceptance/verifier all exited 0. Runtime-owned BattleLab report and exported-player report both identify `BattleLab`/`battlelab.m1.melee`, `passed=true`, `unexpectedErrors=0`, input digest `65ff279dac01cc31305336485af41cb595c37137edd25e04753aa5c62ec84787`, transcript digest `75dc2d6f0dda9fc71c364a5c265f1c640ce7bc949435f7522dc08896272faf58`, result/digest `7faa2ec40c0830f317f4c87296c2e54c27d6f43def9b18f92d113834e982da1c`, `SideAWin`, terminal tick 576, 100 survivors, 28 casualties, and 49 retreated units. Both reports prove `skipWatchEquivalent=true` and `resolutionIdentityShared=true`; existing M1.3 counters remain 128 spawned, 134/134 keyframes, 1 contact, 660 attacks, 660 damage events, 28 deaths/remains, 1 rout, 7 control transitions, result shown, and playback complete.
- The final direct release export used the pinned templates and produced `acceptance-3/export/Warwrought.exe`, matching `.pck`, and `data_Warwrought_windows_x86_64` with 192 files including all required managed outputs. `Start-Process -FilePath` launched that exact same-run executable with `--headless`, explicit player log/report, `battlelab.m1.melee`, `-PassThru -Wait`; player PID 31036 exited 0, evidence was finalized before the existing verifier, and the player verifier exited 0. The same exported player also passed the retained `bootstrap.m0` smoke (PID 18860, verifier 0); no player processes remained afterward.
- Timing evidence uses the actual stopwatch around the one committed resolver call, not playback time, and the retained transcript's `576 / 12` ticks-per-second nominal 1x duration: direct BattleLab samples were 41.8525 ms (`iteration-1`) and 41.732 ms (`acceptance-3`) against 48,000 ms (about 1,150x faster); the final exported-player report measured 39.0573 ms (about 1,229x faster). Each is below the required 9,600 ms one-fifth threshold. No benchmark framework or simulation optimization was added.
- Required negative paths remain proven under `acceptance-3/negative/`: Bootstrap injected unexpected error kept process exit 0 but the verifier returned 1; invalid BootstrapLab scenario and unsupported BattleLab scenario returned process/verifier exit 1 with failed reports. Their `ERROR` log records are intentional negative evidence, not nominal-run health failures. The visible pinned GUI BattleLab run is under `acceptance-3/gui-observation/`: it showed `Warwrought (DEBUG)` with a non-zero window handle, ran the production scene for 5 seconds, logged Forward+ and the BattleLab-ready surface, and closed normally with exit 0.
- Known limitations remain the bounded M1 fixture and M1.3 simple flat presentation; terrain/foliage/height/camera scale, broader combat, campaigns, saves, replay persistence, and later-milestone systems remain deferred.

## 2026-08-12 — M2 Procedural 2.5D Battlefield Planning

- Status: Planning complete; no M2 production implementation, runtime verification, or M3 planning was performed. The committed M0/M1 contracts remain the foundation, including the frozen operations/export workflow, canonical M1 digests, one committed resolution, and presentation-only transcript playback.
- Added `plans/m2/` with a three-task sequence: M2.1 (**READY**) establishes deterministic battlefield/height-field data without changing M1 commitment; M2.2 integrates low-poly terrain, sampler-driven billboard elevation, decorative foliage/props, and production camera; M2.3 creates the production 100/300/500 scale measurement path and M2 final gate.
- M2 explicitly keeps terrain mesh/collision, foliage, camera, and render time non-authoritative. The unchanged M1 fixture selects M2 environment data alongside—not inside—its canonical `BattleDefinition`; future terrain mechanics require a separately planned authoritative extension.
- The M2 scale baseline is ordinary centralized `Sprite3D`/Node3D presentation. M2.3 records runtime-owned spawn/frame/memory/node/effect metrics before any rendering escalation. Median sampled playback frame time above 33.3 ms, p95 above 50 ms, broken playback/control observation, or unbounded memory is an evidence-only escalation trigger for Planner, not authorization for MultiMesh/ECS/custom rendering/threading.
- M2 requires direct production and visible Forward+ evidence, exported-player proof at the final gate, and protected M1 Bootstrap/BattleLab canonical/negative-path regressions. Procedural campaign/province linkage, terrain combat effects, M3 doctrine/ranged/abilities, factions/AI/content, saves/replay persistence, final authored art, and optimization implementation remain deferred.

## 2026-08-12 — M2.1 Deterministic Battlefield Data and Height Field

- Status: Builder implementation complete; M2.1 remains limited to deterministic battlefield data and its required pure-test/runtime regression proof. Added immutable `BattlefieldDefinition`, bounded `BattlefieldGenerationRequest`/`BattlefieldGenerationParameters`, explicit `BattlefieldBounds`/`BattlefieldResolution`, row-major integer height and terrain-region samples, explicit deployment rectangles plus usable-margin insets, `BattlefieldHeightSampler`, CPU-only `BattlefieldGenerator`, and the two fixed local profiles in `src/Battle/Model/`.
- Fixed profiles are `battlefield.m2.open-meadow` seed `0x4D324D4541444F57` (`5562593449599061847`) and `battlefield.m2.broad-highland` seed `0x4D324849474C4E44` (`5562587969321979460`), both bounds X `[-60000,60000]` / Z `[-40000,40000]`, resolution `17x13`, and explicit `deployment.side-a` / `deployment.side-b` rectangles. Open-meadow generation parameters are average `5500`, roughness `240‰`, broad frequency `2`, flatten radius `8000`, usable margin `4000`, elevation `[0,18000]`; broad-highland is average `7500`, roughness `520‰`, broad frequency `4`, flatten radius `7000`, usable margin `4000`, elevation `[0,22000]`.
- Representative fixed outputs: open-meadow digest `567f72c3d9e754722839fb8f1fb034fea7d9f647bc66690a36416ed0e7dc5443`, center sample `(xIndex=8,zIndex=6)=1815`, sample range `[0,12017]`, query `(12345,-2345)=3482 / terrain.low`; broad-highland digest `5973015037208b1d09e4d50be3e0353b3bf200dd06fd1a1ccdf77ec4f691326f`, center sample `10156`, sample range `[0,19502]`, same representative query `7492 / terrain.middle`. Digests include schema, ID, bounded seed, bounds/resolution, generation inputs, ordered region IDs/samples, ordered heights, and ordered deployment/usable bounds through the existing primitive `CanonicalDigestWriter`.
- Sampler convention is documented in `BattlefieldHeightSampler`: regular grid endpoints lie on inclusive X/Z bounds; height uses row-major integer bilinear interpolation (X then Z, floor division); region uses nearest sample per axis with exact half-way ties upward; out-of-bounds X/Z independently clamp to the nearest inclusive bound and report `WasClamped`; all height values remain bounded integer units and derived metres are finite. No height is fed into M1 combat or authoritative battle motion.
- Generation uses explicit seeded integer lattice value-noise composition for broad hills, seeded signed per-sample roughness, bounded clamping, and deterministic rectangle-distance deployment flattening. It does not use `System.Random`, engine/Godot RNG or noise, GPU/render state, timestamps, files, hash ordering, culture formatting, mesh/physics/raycast/NavMesh, or any Godot API. Terrain remains M2 presentation-ready data only and has no M2 combat effect.
- Added `tests/Warwrought.Tests/M2BattlefieldTests.cs`: repeated profile/data/digest/query equality; changed seed/profile divergence; bounds/corner/edge/interior/out-of-bounds semantics; finite results; explicit usable deployment/flattening checks; caller collection mutation/read-only ownership checks; malformed schema/seed/bounds/sample/region/deployment/generation validation; culture independence; fixed-output digest evidence; and M1 input/transcript/result/one-commit/skip-watch regression identities. Focused M2 tests passed `8/8`; full approved test project passed `35/35`; `dotnet build --nologo` passed with `0 Warning(s), 0 Error(s)`.
- Direct production regressions passed twice from fresh ignored roots `artifacts/local/m2.1/acceptance-1/` and `acceptance-2/`: pinned Godot 4.7.1 console `BootstrapLab.tscn` `bootstrap.m0` exited `0`, and existing `scripts/verify-bootstraplab.ps1` exited `0` in both runs; both reports identify `BootstrapLab`/`bootstrap.m0`, `passed=true`, `unexpectedErrors=0`, and both inspected five-line logs are clean. Pinned Godot console `BattleLab.tscn` `battlelab.m1.melee` exited `0`, and the same verifier exited `0` in both runs; both reports identify `BattleLab`/`battlelab.m1.melee`, `passed=true`, `unexpectedErrors=0`, input `65ff279dac01cc31305336485af41cb595c37137edd25e04753aa5c62ec84787`, transcript `75dc2d6f0dda9fc71c364a5c265f1c640ce7bc949435f7522dc08896272faf58`, result `7faa2ec40c0830f317f4c87296c2e54c27d6f43def9b18f92d113834e982da1c`, `skipWatchEquivalent=true`, `resolutionIdentityShared=true`, and both inspected five-line logs are clean. No terrain runtime hook was added; M1 canonical identities, resolver lifecycle, verifier, scenes, and acceptance contract were unchanged.
- Review-process correction: after all other review activity and immediately before its final verdict, Reviewer must run `git diff --check` from the repository root and report its actual exit code. Planner must independently run and report the same final check before declaring a task ready for Git Steward; Git Steward retains its own pre-commit check as the transaction backstop. This records process discipline only and does not change any normative task acceptance criterion.

## 2026-08-12 — M2.2 Production Terrain, Foliage, and Camera Integration

- Status: Builder implementation and verification complete for the committed M2.2 contract; no M2.3 scale/profiling, M3, campaign, Codex, generated art, export/player, Reviewer, or Git Steward work was started.
- Production `BattleLab.tscn` now retains its existing M1 roots and adds the `BattlefieldTerrainView`/`BattleCameraRig` responsibilities. `BattleLab` selects M2 profiles alongside the unchanged M1 fixture: `battlefield.m2.open-meadow` seed `5562593449599061847` / digest `567f72c3d9e754722839fb8f1fb034fea7d9f647bc66690a36416ed0e7dc5443`, and `battlefield.m2.broad-highland` seed `5562587969321979460` / digest `5973015037208b1d09e4d50be3e0353b3bf200dd06fd1a1ccdf77ec4f691326f`. The declared acceptance scenarios are `battlelab.m2.open-meadow` and `battlelab.m2.broad-highland`; normal visible launch can select a profile with `--battlefield-profile` without entering acceptance mode.
- `BattlefieldTerrainMeshData` builds the deterministic 17x13 coarse grid as 384 consistently wound triangles / 1,152 duplicated vertices. `BattlefieldTerrainPresenter` consumes this input through Godot 4.7.1 `ArrayMesh.AddSurfaceFromArrays` with explicit normals and vertex colors/material for faceting. No terrain collision or mesh/raycast height lookup exists. Runtime reports prove `terrainMeshSamplerAgreement=true` for both profiles.
- `BattlefieldPresentationProjector` is the single production X/Z-to-`Vector3` edge. Active units, remains, effects, contact marker, mesh vertices, and decoration placement use the M2.1 `BattlefieldHeightSampler`; only documented pivot offsets are applied (`unit=0.88m`, `remains=0.18m`, `effect=0.46m`, contact `0.05m`). Runtime projection counters are derived from actual playback/scene creation, with no per-unit process or scene-tree lookup.
- `BattlefieldFoliageScatter` uses the documented presentation seed salt `0xBA7710F0114E`, ordered deterministic placements, bounded scale/tint/flip variation, deployment/contact exclusions, billboard tree/bush placeholders, and simple non-colliding rock props. Focused tests prove repeatability/bounds/exclusion; runtime reports prove 30 placements and profile-specific placement digests (`adda947b93cd28b4d1170138d5538b419dc4237efcc70f6f19b8baa516c863e6` meadow; `9a12bd67baf35b084e0fc6c0ee8fa22c6bc9e40ea8706f359d7d239fb3d83d83` highland).
- `BattleCameraRig` is an orthographic, presentation-only rig with bounded pan, zoom, reset framing, keyboard/mouse controls (`WASD`/arrows, wheel or +/- and `R`), and HUD buttons. Acceptance exercises pan/zoom/reset and checks retained transcript/result digests. Forward+ GUI normal launches on the pinned GUI executable showed `Warwrought (DEBUG)` with non-zero handles for both meadow and highland profiles, D3D12 Forward+ logs, visible BattleLab production surface, M2 terrain HUD, billboard formations/foliage/props, and clean normal closes. An explicit control observation sent `W`, wheel-up, and `R`; runtime camera evidence reports orthographic/pan/zoom/reset observed.
- Runtime-owned reports/verifier were extended minimally for M2 profile, battlefield digest, mesh topology/sampler agreement, foliage/props, projection counters, and camera evidence. M1 verifier values remain protected. Final direct pinned-console evidence is under ignored `artifacts/local/m2.2/final-direct-1/`: BootstrapLab nominal, canonical `battlelab.m1.melee`, meadow, and highland all exited 0 and shared verifier exited 0. All reports have `passed=true`, `unexpectedErrors=0`, and clean inspected logs. M1 retained fields in every BattleLab report remain input `65ff279dac01cc31305336485af41cb595c37137edd25e04753aa5c62ec84787`, transcript `75dc2d6f0dda9fc71c364a5c265f1c640ce7bc949435f7522dc08896272faf58`, result `7faa2ec40c0830f317f4c87296c2e54c27d6f43def9b18f92d113834e982da1c`, `SideAWin`, tick `576`, survivors `100`, casualties `28`, retreated `49`, `skipWatchEquivalent=true`, and `resolutionIdentityShared=true`.
- Focused M2.2 test coverage is `M2BattlePresentationTests` (4/4), including mesh topology/sampler projection, foliage stability/exclusion, and camera/presentation digest immutability. Focused M2/M1 regression selection passed 25/25; final full test project passed 39/39; `dotnet build --nologo` passed with 0 warnings/0 errors. Bootstrap injected unexpected-error and unsupported BattleLab negative semantics were rerun under ignored `artifacts/local/m2.2/final-negative-1/`: injected process exit 0 but verifier exit 1; unsupported process/verifier exit 1. Negative `ERROR` output is intentional evidence only.
- GUI visual evidence is retained under ignored `artifacts/local/m2.2/gui-meadow-proof-1/`, `gui-highland-proof-1/`, `gui-meadow-terminal-proof-2/`, and `gui-highland-terminal-proof-2/`: actual pinned Forward+ windows were visibly inspected at non-zero handles, with captures showing faceted terrain, distinct billboard formations, foliage/rock placeholders, terrain HUD, contact marker, lighting, and the M2 camera control surface. Captured control observations sent `W`, wheel-up, and `R`; both GUI profile runs closed normally with exit 0 and clean Forward+ logs. The terminal captures show meadow at tick 550/576 with 660 attacks/damage, 28 deaths, 23 remains in the final death-presentation window, one rout, and highland at `BattleEnded`, `SideAWin`, tick 576, 134/134 keyframes, 660 attacks/damage, 28 deaths/remains, one rout. Runtime-owned reports/verifiers for both GUI acceptance runs passed 0. No warnings or unexpected errors appeared in nominal direct/GUI logs. Final repository-root `git diff --check` and `git status --short` remain to be run after this entry.
