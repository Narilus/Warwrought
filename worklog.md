# Warwrought Worklog

## 2026-08-09 — M0.1 Project Foundation

- Status: Complete; M0.1 acceptance verification succeeded.
- Established the pinned Godot 4.7.1 .NET/C# project identity, Forward+ feature, desktop window baseline, candidate Windows Desktop preset, root solution/project, initial M0 directories, C# conventions, and ignore policy.
- `dotnet build --nologo` and `dotnet build Warwrought.sln --nologo` both succeeded with 0 warnings and 0 errors; the solution lists `Warwrought.csproj`.
- Pinned console `--headless --editor --import`, pinned GUI `--headless --editor --import`, pinned console `--build-solutions --quit`, and a visible pinned GUI `--editor --import` all exited 0. The visible import reported `D3D12 12_0 - Forward+`; final import/build logs contain no unexpected error-pattern matches.
- Evidence is under ignored `artifacts/local/m0.1/` (`dotnet-build*.log`, `dotnet-solution-list.log`, and Godot import/build logs). Commands remain provisional; M0.4 owns command freezing.
- Export execution, player smoke, BootstrapLab, runtime reports, acceptance scanners, tests, and gameplay were intentionally not performed; those belong to later tasks/non-goals.
- An intentionally early exploratory GUI `--quit-after 1` invocation emitted Godot's `Scan thread aborted` warning; it was not used as acceptance evidence and was superseded by the clean supported `--import` run.

## 2026-08-09 — M0.2 BootstrapLab Runtime Contract

- Status: Complete; the M0.2 production-scene, report/log, verifier, layout, and fast-test acceptance evidence succeeded. M0.3 export/player smoke and M0.4 command freezing were not started.
- Added the maintained production `scenes/Labs/BootstrapLab.tscn` with the C# `Warwrought.Bootstrap.BootstrapLab` node, ordinary visible identifying surface, bounded `bootstrap.m0` arguments, runtime-owned versioned JSON report, intentional acceptance exit, and acceptance-only `--inject-failure=unexpected-error` path. Added `Warwrought.Core.StableId` and pure argument/report validation used by the test project.
- Completed the Section 3 source/layout folders with tracked `.gitkeep` placeholders only where the folders remain intentionally empty. Added the single mainstream xUnit project at `tests/Warwrought.Tests/`, referencing `Warwrought.csproj`; the production project excludes test source files.
- Candidate build/test commands (still provisional): `dotnet build --nologo` succeeded with 0 warnings/0 errors; `dotnet test tests\Warwrought.Tests\Warwrought.Tests.csproj --nologo` passed 3/3 tests.
- Candidate production acceptance command shape, using the pinned console executable, was:
  `D:\Dev\Godot\Godot_v4.7.1\Godot_v4.7.1-stable_mono_win64_console.exe --headless --path D:\Dev\Projects\warwrought --scene res://scenes/Labs/BootstrapLab.tscn --log-file D:\Dev\Projects\warwrought\artifacts\local\m0.2\bootstrap-final.log -- --acceptance=bootstrap.m0 --report=D:\Dev\Projects\warwrought\artifacts\local\m0.2\bootstrap-final.json`
  It exited 0, the transparent verifier command `pwsh -NoProfile -File scripts\verify-bootstraplab.ps1 -ReportPath artifacts\local\m0.2\bootstrap-final.json -LogPath artifacts\local\m0.2\bootstrap-final.log` exited 0, and the report/log are under ignored `artifacts/local/m0.2/`.
- Final clean report fields: `schemaVersion=1`, `scenario=bootstrap.m0`, `scene=BootstrapLab`, `godotVersion=4.7.1-stable (official)`, `buildRuntimeIdentifier=Warwrought@0.1.0.0`, `runtimeIdentifier=win-x64`, `projectIdentity=Warwrought`, `projectVersion=0.1.0`, `unexpectedErrors=0`, `passed=true`. The explicit log contains the runtime report-write, clean-result, and intentional-exit evidence with no documented unexpected patterns.
- Pinned GUI normal launch used the same production scene and explicit log at `artifacts/local/m0.2/bootstrap-visible-normal.log`; it created a visible window (`Warwrought (DEBUG)`, non-zero window handle), closed cleanly with process exit 0, and logged `BootstrapLab ready: maintained production C# Node, normal launch mode.` plus `D3D12 12_0 - Forward+`.
- Exact provisional GUI command exercised for that visible run:
  ```powershell
  $gui = 'D:\Dev\Godot\Godot_v4.7.1\Godot_v4.7.1-stable_mono_win64.exe'; $root = 'D:\Dev\Projects\warwrought'; $log = 'D:\Dev\Projects\warwrought\artifacts\local\m0.2\bootstrap-visible-normal.log'; $process = Start-Process -FilePath $gui -ArgumentList @('--path', $root, '--scene', 'res://scenes/Labs/BootstrapLab.tscn', '--log-file', $log) -PassThru; Start-Sleep -Seconds 4; $observed = Get-Process -Id $process.Id -ErrorAction SilentlyContinue; if ($null -eq $observed) { 'visible_process_not_found'; exit 1 }; "visible_process_id=$($observed.Id)"; "visible_window_title=$($observed.MainWindowTitle)"; "visible_window_handle=$($observed.MainWindowHandle)"; "visible_has_window=$($observed.MainWindowHandle -ne 0)"; $windowObserved = $observed.MainWindowHandle -ne 0; $closed = $process.CloseMainWindow(); "close_main_window_requested=$closed"; if (-not $process.WaitForExit(5000)) { Stop-Process -Id $process.Id -Force; "visible_process_force_stopped=$true" } else { "visible_process_exit_code=$($process.ExitCode)" }; if (-not $windowObserved) { exit 1 }; exit 0
  ```
  It exercised the pinned GUI executable against production `res://scenes/Labs/BootstrapLab.tscn`, wrote the explicit ignored log above, observed `Warwrought (DEBUG)` with a non-zero window handle, and exited 0 after a normal close.
- The real-runtime negative command added `--inject-failure=unexpected-error`, emitted `bootstrap-injected-failure.json` with `passed=false`, `failureCategory=InjectedUnexpectedError`, and `unexpectedErrors=1`, and emitted the diagnostic `ERROR`/C# backtrace in `bootstrap-injected-failure.log`. Godot intentionally exited 0 to prove the verifier does not trust process status; the verifier returned 1 and printed both the failed-report and log-pattern reasons. A subsequent `bootstrap-final` acceptance run passed cleanly.
- Invalid scenario arguments were also exercised: `bootstrap-invalid-args.json` recorded `passed=false`/`InvalidArguments`, Godot exited 1, and the verifier returned 1. No reports/logs/build outputs or generated C# UID sidecars are tracked; `.godot/`, `*.cs.uid`, `bin/`, `obj/`, and local artifacts remain ignored.
